# Phase 003 — Showcase

**Goal:** Replace the Work placeholder with the real Showcase: WRDB flagship with
a pinned GSAP scroll sequence, plus minimal migraine/TravelGuide cards. Real facts
only — no invented screenshots, features, or links.
Ref: `docs/superpowers/specs/2026-07-16-portfolio-design.md` §4–5.

## Tasks

### Data
- [ ] `lib/projects.ts` — WRDB (rich: chapters, stack, links) + migraine/TravelGuide
      (minimal). Links: wrdb.site real; App Store label no href; repos deferred.

### Flagship (the "wow")
- [ ] `ShowcaseFlagship` (client, GSAP ScrollTrigger): pin a stylized device frame
      while WRDB "chapters" (What it is / How it's built / Applied AI) scroll past;
      subtle scrub animation on the device. Wire ScrollTrigger to Lenis.
- [ ] `gsap.matchMedia`: pin only on `md+` AND `prefers-reduced-motion: no-preference`.
      Mobile / reduced-motion → static stacked layout (no pin), fully readable.
- [ ] `DeviceFrame`: CSS iPhone frame with a monochrome stylized placeholder + a
      clear slot to drop a real screenshot later. NOT a fabricated app screen.

### Minimal cards
- [ ] migraine + TravelGuide: name + honest one line ("Personal iOS project") +
      "details soon". No invented links/features.

### Wire + verify
- [ ] `Showcase` section (id="work") in page.tsx.
- [ ] `npm run build` clean; production smoke test (GSAP pin works, no console
      errors); Playwright desktop + mobile + reduced-motion path; commit.

## Out of scope
Blog (004), contact form (005). App Store / repo hrefs stay deferred.
