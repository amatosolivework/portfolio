# Phase 002 — Hero (typographic + shader) & About + CV

**Goal:** Replace the hero placeholder background with the signature animated
shader field; build the About + CV section with real content and the downloadable
CV. Ref: `docs/superpowers/specs/2026-07-16-portfolio-design.md` §3–5.

## Tasks

### CV asset
- [ ] Copy `../Practicas UB/cv-en.pdf` → `public/cv.pdf` (CV button already points here).

### Shader hero (the signature)
- [ ] `ShaderField` client component (OGL): slow, low-contrast animated
      grain/gradient/noise behind the hero type. Monochrome — tuned to paper/ink.
- [ ] Lazy-load (`next/dynamic`, `ssr:false`); render nothing until mounted so it
      never blocks paint. Static CSS radial gradient remains as the fallback layer.
- [ ] Guard: skip WebGL when `prefers-reduced-motion` or no WebGL context; pause
      on tab blur / when offscreen (visibilitychange) to save battery.
- [ ] Promote `HeroPlaceholder` → real `Hero` (keep mask reveals, magnetic CTAs).

### About + CV
- [ ] `About` section (real data from cv-en.md / profile.md only):
      bio, UB education (BSc Comp Eng, Sep 2023–Jun 2027, SW Eng spec),
      skills grouped with mono chips, languages, CV download button.
- [ ] Layout: editorial two-column (narrative + structured facts), mono labels,
      scroll reveals. No invented facts.

### Verify
- [ ] `npm run build` clean; Playwright screenshots (hero desktop/mobile, About);
      confirm reduced-motion path (static fallback, no WebGL).
- [ ] Commit `feat(02): shader hero and about/cv section`.

## Out of scope
Showcase/GSAP (003), blog (004), contact form (005). App links stay deferred.
