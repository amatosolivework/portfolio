# Phase 005 — Contact & footer

**Goal:** Replace the Contact placeholder with a real, distinctive contact
section — direct links only (no form, per Alex). Footer already exists (001).
Ref: spec §5, ROADMAP #5.

## Tasks
- [ ] `components/site/contact.tsx` (id="contact"): eyebrow "03 · Contact", big
      statement, real availability line (curricular internship, mornings,
      Barcelona — from cv-en.md), prominent email mailto (magnetic), LinkedIn +
      GitHub links. Real data from `lib/site.ts`. Scroll reveals, mono signature.
- [ ] Wire `<Contact />` into page.tsx; drop now-unused Section/Reveal imports.
- [ ] Verify: build clean, prod smoke test, Playwright desktop + mobile, links
      resolve (mailto/LinkedIn/GitHub), 0 console errors, commit.

## Out of scope
Contact form (explicitly dropped). OG/analytics/favicon → Phase 006.
